#include <stdio.h>      // I always include these 2 h files.  In the shell, just run "man stdio" or 
#include <stdlib.h>     // "man stdlib" to see the manuals for these.

/* main() should always return an int, instead of void.
 * I believe this is a requirement in the C standard 
 */
int main()
{
    int ret = 0;

    return ret;
}



/*
 * ----------------------------------------------
 * To compile this:
 *
 *     $ g++ ./00_bare_minimum_c_app.c
 *
 *     This generates a file that you can run called:
 *     a.out
 *
 *     To run this file:
 *     $ ./a.out
 *
 *    That's it!
 */
